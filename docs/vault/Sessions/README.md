# Sessions

Optional. One file per work session worth remembering later — not a log of every
session. Suggested name: `YYYY-MM-DD-topic.md`, a few lines: what was worked on, what
changed, what's left. Only write one when there's something a future session would
actually benefit from knowing that isn't already captured in a Decision or Learning.
